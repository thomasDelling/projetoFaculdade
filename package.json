{
  "name": "arenaquizz",
  "version": "1.0.0",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.15.2",
    "react-router-dom": "^7.6.2"
  },
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend/auth-service && npm install && cd ../backend/user-service && npm install && cd ../backend/quiz-service && npm install",
    "start": "concurrently \"npm run start-auth\" \"npm run start-user\" \"npm run start-quiz\" \"npm run start-frontend\"",
    "start-auth": "cd backend/auth-service && node index.js",
    "start-user": "cd backend/user-service && node index.js",
    "start-quiz": "cd backend/quiz-service && node index.js",
    "start-frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^3.1.10"
  }
}
